# Зависимости сборки (esbuild и пр.) — не коммитим, ставятся через `npm install`
/node_modules/

# Системный мусор macOS
.DS_Store

# Примечание: собранный assets/scripts/js/woo.bundle.js КОММИТИТСЯ (вариант 1),
# чтобы на сервере не требовался Node.
